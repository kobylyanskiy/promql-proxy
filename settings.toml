[server]
listen_address = "0.0.0.0:8081"
log_level = "info"
timeout_seconds = 60

[routing]
target_label = "env"
fallback_url = "http://mimir-gateway.monitoring.svc.cluster.local:8080/prometheus"
# default_tenant = "anonymous"

[tenants]
# Format: "label_value" = "mimir_tenant_id"
prod = "production"
staging = "staging"
dev = "development"
